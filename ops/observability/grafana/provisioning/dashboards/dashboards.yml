apiVersion: 1

providers:
  - name: "nextstop-local"
    orgId: 1
    folder: "NextStop Local"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
